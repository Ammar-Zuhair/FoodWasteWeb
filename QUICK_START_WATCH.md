# 🚀 دليل البدء السريع - Watch Mode

## ✅ النظام جاهز ويعمل الآن!

Watch Mode يعمل في Terminal ويراقب التغييرات تلقائياً.

## 📋 الخطوات التالية:

### 1. افتح Android Studio
- افتح مشروع Android في: `frontend/android/`
- انتظر حتى يكتمل Gradle Sync

### 2. ابدأ التعديل
- عدّل أي ملف في `src/` (مثلاً `src/layouts/MobileLayout.jsx`)
- Watch Mode سيكتشف التغيير تلقائياً

### 3. انتظر المزامنة
- بعد 3 ثوانٍ من آخر تغيير
- ستتم المزامنة تلقائياً مع Android Studio

### 4. في Android Studio
- إذا ظهر إشعار "Gradle files have changed"
- اضغط "Sync Now" أو اضغط `Ctrl+Shift+O`

## 🎯 مثال سريع:

1. **عدّل ملف**: `src/layouts/MobileLayout.jsx`
2. **احفظ الملف**: `Ctrl+S`
3. **انتظر**: 3 ثوانٍ
4. **شاهد**: Watch Mode يقوم بالمزامنة تلقائياً
5. **في Android Studio**: اضغط `Ctrl+Shift+O` للمزامنة

## 📱 تشغيل التطبيق:

### في Android Studio:
1. اختر جهاز (Emulator أو Device)
2. اضغط Run (▶️) أو `Shift+F10`
3. التطبيق سيتم تشغيله مع آخر التغييرات

### أو من Terminal:
```bash
cd frontend/android
.\gradlew installDebug
```

## 🔧 الأوامر المفيدة:

| الأمر | الوصف |
|------|-------|
| `npm run watch:sync` | تشغيل Watch Mode |
| `npm run build` | بناء المشروع |
| `npm run cap:sync` | مزامنة يدوية |
| `npm run cap:open:android` | فتح Android Studio |

## ⚠️ ملاحظات مهمة:

1. **Watch Mode يجب أن يعمل**: اترك Terminal مفتوحاً
2. **Android Studio Sync**: قد تحتاج Sync يدوي أحياناً (`Ctrl+Shift+O`)
3. **إعادة بناء APK**: فقط عند تغيير plugins أو config

## 🎉 جاهز للاستخدام!

افتح Android Studio وابدأ التعديل - كل شيء سيعمل تلقائياً!













